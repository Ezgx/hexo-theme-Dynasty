-
  var options = {
    prev_text: '<i class="dys arrow-left"></i>',
    next_text: '<i class="dys arrow-right"></i>',
    mid_size: 1,
    escape: false
  }

if is_post()
  block content
else
  nav#pagination
    .pagination
      if is_home()
        - options.format = 'page/%d/'
      !=paginator(options)